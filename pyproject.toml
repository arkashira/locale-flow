[project]
name = "locale-flow"
version = "0.1.0"
description = "A minimal helper for integrating Locale‑Flow into CI pipelines."
readme = "README.md"
requires-python = ">=3.8"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = ["tests"]
